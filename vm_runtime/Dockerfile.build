FROM ubuntu:24.04

RUN apt-get update && apt-get install -y \
    build-essential git curl python3 debootstrap qemu-utils firecracker

WORKDIR /app
COPY . .

RUN chmod +x build/*.sh vm/*.sh rootfs/* setup-runtime.sh 2>/dev/null || true

CMD ["/bin/bash"]